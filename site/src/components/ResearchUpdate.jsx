import { HEADLINE } from "../data.js";
import { fmtPct, fmtScore } from "../format.js";

export default function ResearchUpdate() {
  return (
    <aside
      id="research-update"
      className="research-update"
      role="note"
      aria-label="Что изменил аудит корпуса"
    >
      <p className="research-update-kicker">Исследование продолжается</p>
      <p className="research-update-lead">
        В первом эксперименте модель правильно определила автора примерно в{" "}
        <strong>{fmtPct(HEADLINE.accuracy, 1)}</strong> случаев.
      </p>
      <p>
        Поздняя проверка корпуса нашла пересечение содержания между несколькими
        произведениями: знакомый фрагмент мог оказаться и среди учебных текстов, и
        среди проверочных. Поэтому {fmtPct(HEADLINE.accuracy, 1)} и macro-F1{" "}
        {fmtScore(HEADLINE.macroF1, 3)} описывают именно первый эксперимент, а не
        окончательную точность метода. Следующий шаг — разделить совпадающие тексты,
        пересобрать корпус и повторить расчёт целиком.
      </p>
      <details>
        <summary>Как проверка сделала исследование строже</summary>
        <p>
          Раньше программа прятала от себя проверяемую книгу. Теперь вместе с ней
          должна исчезать вся группа текстов с тем же содержанием — даже если рассказ
          отдельно входит в сборник под другим названием. Это не меняет сохранённые
          результаты первого опыта, но не позволяет выдавать их за финальную оценку.
        </p>
      </details>
    </aside>
  );
}
