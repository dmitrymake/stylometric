"""Retired anomaly diagnostic for unauthenticated loose vector artifacts."""
from __future__ import annotations


def main() -> None:
    raise SystemExit(
        "scripts/statistic/anomaly_stats.py is retired with "
        "data/train_vectors.pkl. A replacement must consume a non-executable, "
        "content-bound representation artifact."
    )


if __name__ == "__main__":
    main()
