# Active release-surface cleanup C1+C2

## Metadata

- Task ID: `2026-08-11-active-surface-cleanup`
- Status: active
- Owner: repository owner
- Created: 2026-08-11
- Baseline commit: `9cfac1badea75f542e619d254dc1623a9e6ede44`
- Target branch/worktree: `main`; one worktree per selected fix from the committed task baseline
- Type: pruning
- Size: M
- Risk: R2
- Risk flags: mutation, external-contract
- Primary domain: site
- Allowed cross-domain: root developer tooling; research-governance release metadata only
- Standard version: 1.3

## 1. Goal and decision

- Desired observable result: the public/release surface has no orphan codebase dumper and exactly
  one supported site package manager, npm, without dependency or generated-site drift.
- Consumer: maintainers, CI, Git-free source archive and GitHub Pages build.
- Decision/process unlocked: smaller active surface and one authoritative JavaScript lock contract.
- Success is not merely: hiding files, weakening lock integrity, or changing package versions.

## 2. Confirmed starting facts

| ID | Fact | Evidence locator | Confidence |
|---|---|---|---|
| F-01 | `generate_codebase.sh` has no tracked consumer and unboundedly scans local `.py`/`.sh` into ignored `codebase.txt`. | script; exact-path Git grep | high |
| F-02 | `codebase.txt` is absent; only the orphan script owns its ignore rule. | filesystem check; `.gitignore:95` | high |
| F-03 | CI, AGENTS and site scripts use npm; no supported command invokes pnpm. | workflow, AGENTS, `site/package.json` | high |
| F-04 | pnpm remains a deliberate second lock/config/test/release binding from historical synchronization. | Git history; lock/test/inventory | high |
| F-05 | npm package-lock already carries exact Babel and optional-platform integrity records. | package-lock and site contract tests | high |

## 3. Unknowns and assumptions

| ID | Unknown/assumption | Why it matters | Resolution | Blocking? |
|---|---|---|---|---|
| U-01 | An external maintainer may prefer pnpm despite no repository command using it. | Removing the lock drops that unsupported convenience contract. | Explicit owner selection C1+C2 accepts npm-only support. | no |
| U-02 | The dumper may be used manually outside tracked consumers. | Deletion removes an unsafe convenience command. | Git history remains; no output exists. | no |

## 4. Scope

### In scope

- C1: delete `generate_codebase.sh` and its now-dead `codebase.txt` ignore rule.
- C2: delete `site/pnpm-lock.yaml`; remove pnpm-only package config and pnpm-specific test code;
  preserve equivalent exact dependency assertions against `package-lock.json`.
- Reconcile release inventory, task result and concise handoff.

### Out of scope

- Dependency/version upgrades, `package-lock.json` regeneration, site source/generated-output changes.
- Deploy/publication, npm publish, external writes or Git push.
- `scripts/` or tracked/ignored `log/` census/deletion.
- Scientific code, corpora, evidence, paired audit, R1/Wikisource control planes.
- Formatter/linter/framework/shared-helper changes or any next cleanup wave.

### Stop / reclassification conditions

- A tracked consumer of C1 or an actual pnpm command is found.
- `npm ci` requires lock regeneration or dependency/version changes.
- Site generated bytes, scientific/release claims, or publication behavior would change.
- Any new dependency/file/entrypoint/framework/state or positive production LOC.

## 5. Frozen acceptance and complexity tripwires

- Max touched production subsystems: two (`root developer tooling`, `site dependency contract`).
- Allowed paths: `.gitignore`, `generate_codebase.sh`, `site/package.json`,
  `site/pnpm-lock.yaml`, `tests/test_site_generator_contract.py`,
  `release/executable_sources.json`, this task and `docs/handoff/CURRENT.md`.
- New production files / production LOC added: 0 / 0.
- Production LOC net target: at most `-19`; only the selected orphan entrypoint is deleted.
- Test LOC added budget: 8; cumulative test LOC must be non-positive.
- Docs/governance LOC added budget: 230 for required task/closeout metadata; correctness overhead is
  not simplification progress.
- New framework/layer/state/dependency/generic abstraction: forbidden.
- New public/operational entrypoint: forbidden; exactly one orphan entrypoint removed.
- Context target: site package-manager authorities 2 -> 1; release archive minus the pnpm lock and
  dumper; no growth in five representative packets.
- Review correction budget: one deletion review and at most one bounded correction.
- Stop/reclassification trigger: any tripwire crossing or supported-consumer counterexample.

## 6. Domain contract

- Primary domain: site.
- Producers / consumers: `package.json` + npm lock own site dependency resolution; workflow consumes
  `npm ci`; release inventory binds shipped support files.
- Allowed external domains: root developer tooling and research-governance release metadata.
- Forbidden areas: scientific/governance claims, corpus/data, evaluator, public rendered content.
- Grain: one operational entrypoint or one package-manager resolution contract.
- Keys/cardinality: one `stylo-article` package, one npm lock, one CI install command.
- Temporal semantics: package versions remain exactly frozen; removed pnpm compatibility is not a
  historical scientific artifact.
- NULL/delete/late-arrival: deleted files remain recoverable from Git history; no runtime state.
- Contract verification: npm install/build, exact lock tests, inventory, release/archive hygiene.

## 6.1. Context locality

Constant method: full required files, physical UTF-8 bytes/lines, token estimate `bytes / 4`.

| Scenario | Baseline packet | Approx tokens | Domain transitions | Target |
|---|---:|---:|---:|---|
| Canonical CLI change | 8 files; 177,939 B; 4,886 lines | 44,485 | 2 | no growth; one root orphan removed |
| v3.2 evaluator change | 10 files; 221,369 B; 4,470 lines | 55,342 | 2 | unchanged |
| Wikisource acquisition | 9 files; 331,742 B; 9,362 lines | 82,936 | 2 | unchanged |
| Real R1 control plane | 16 files; 647,671 B; 17,971 lines | 161,918 | 3 | unchanged |
| Public claim/site change | 8 files; 280,361 B; 7,926 lines | 70,090 | 2 | package-manager source count 2 -> 1 |

- Central-config-only reads: release inventory and site lock contract.
- Duplicate/conflicting sources: npm and pnpm locks for an npm-only workflow.
- Blurred boundary: root dumper scans ignored/local research code outside reviewed release roots.
- Rejected/excluded artifacts: tracked/ignored `log/`, scientific evidence, control planes.

## 7. Invariants

- INV-01: npm dependency names, specs, resolved versions, integrity and optional platform closure do
  not change.
- INV-02: site generator/build/render contract and generated bytes do not change.
- INV-03: Git-free archive and executable inventory remain closed and private-path-free.
- INV-04: no scientific, registry, freeze, authorization, execution or publication status changes.
- INV-05: only selected C1/C2 paths change; no automatic next wave.

## 8. Phases and gates

- Read-only audit and owner selection C1+C2 completed on 2026-08-11.
- Commit this contract as the common baseline; implement C1/C2 in separate worktrees.
- Integrate accepted commits; reconcile inventory; perform clean-context combined deletion review.
- After review, run frozen full verification and metadata-only closeout.
- Allowed blockers: AC/invariant regression, supported consumer, safety gate or tripwire.

## 9. Acceptance criteria

- [ ] AC-01 Orphan dumper and dead ignore rule are absent; no `codebase.txt` appears.
- [ ] AC-02 npm is the sole package-manager contract; all pnpm live references are absent.
- [ ] AC-03 Package names/specs/resolved versions and package-lock bytes are unchanged.
- [ ] AC-04 Exact Babel and optional-platform lock assertions remain executable.
- [ ] AC-05 npm install/build, focused/full regression and release/archive gates pass.
- [ ] AC-06 Net production LOC is negative; no new files/concepts/dependencies/entrypoints.
- [ ] AC-07 Scientific and production/publication state is unchanged.

## 10. Verification plan

- Targeted: site generator contract, governance, executable inventory and release integrity.
- Runtime: `npm ci --no-audit --no-fund`, `npm run gen`, `npm run build`; generated-tree diff clean.
- Regression: full pytest; py_compile; provenance; checkout and Git-free archive hygiene.
- Negative: grep live pnpm/dumper references; verify package-lock and generated bytes against baseline.
- Cannot-run: record separately; do not infer runtime pass from static checks.

## 11. Documentation impact

- ADR/domain/runbook: none; support surface is reduced without architecture/scientific change.
- Handoff: metadata-only closeout.

## 12. Result

- Pending implementation, review and verification.

## 13. DoD references

- [ ] DOD-01 through DOD-05
- [ ] DOD-07 through DOD-15
- DOD-06 not applicable unless risk changes.
