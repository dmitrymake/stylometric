# Sholokhov reference-circularity wording

## Metadata

- Task ID: `2026-08-23-sholokhov-reference-circularity-wording`
- Status: active
- Owner: Dmitry Purtov (repository owner)
- Created: 2026-08-23
- Baseline commit: `67afbb65678698d9ab4aa4168d58748bb6983d4d`
- Target: attached `main`; local commits only
- Type / Size / Risk: implementation / M / R2
- Risk flags: mutation, scientific-claim
- Primary domain: reporting/publication
- Allowed cross-domain: site (one claim source); evaluation evidence (read-only aggregate contract)
- Standard: `docs/agentic/STANDARD.md` v1.3

## 1. Goal and decision

- Observable result: the public Sholokhov chapter distinguishes exclusion of tested works from
  training from the residual reference-label circularity of early Don anchors.
- Consumer: readers of the static article and future publication review.
- Decision unlocked: retain the directional `3/4 → Шолохову` result with its actual scope, without
  representing early reference labels as independently settled evidence.
- Success is not a new attribution result, weaker/stronger numerical verdict, artifact correction,
  model rerun, generated-data change, deployment, push, or publication.

The operator explicitly prioritized factual/model accuracy and autonomous bounded fixes. This task
changes only local publication source; no external write/deploy occurs. Any actual publication action
would be a separate R3a gate with immediate human approval.

## 2. Confirmed starting facts

| ID | Fact | Evidence | Confidence |
|---|---|---|---|
| F-01 | All four tested TD volumes are held out by the active LBO artifact; active verdict is `3/4`. | `docs/sholokhov_lobo.json:8-18,117-183`; generator contract test | high for artifact declaration |
| F-02 | Training retains `rodinka`, `zherebenok`, `batraki`, labelled only `anchor_solo_in_train`. | `docs/sholokhov_lobo.json:19-23` | high |
| F-03 | The public page itself says the early 1924–26 Don period is disputed and no undisputed same-period Don anchor exists. | `site/src/sections/Sholokhov.jsx:809-815` at baseline | high |
| F-04 | Active rigor conclusion retains “циркулярность эталона”. | `docs/sholokhov_rigor12.json:41-72` | high |
| F-05 | Ten public loci call early anchors undisputed or claim no closed circle; existing tests bind numbers but not this semantic distinction. | bounded source inventory + `test_site_generator_contract.py:290-316` | high |

## 3. Interpretation and unknowns

- Observed: declared split excludes exact tested TD/control works.
- Interpretation: target/work leakage is excluded according to the aggregate artifact; this does not
  independently certify the authorship labels of remaining reference works.
- Confidence: high for the internal artifact/page contradiction; medium for actual runtime leakage
  absence because this task does not reopen raw data or recompute the split.
- Falsifier: a tested TD/content duplicate in real train invalidates even the narrowed claim; an
  independent provenance decision establishing all early anchors outside the disputed assumption
  removes the residual-label caveat. Neither evidence is present in the active packet.

## 4. Scope

### In scope

- `site/src/sections/Sholokhov.jsx`: wording only at the ten inventoried loci.
- `tests/test_site_generator_contract.py`: one static semantic reconciliation gate.
- This task result and handoff.

### Out of scope

- Every number, data expression, result direction/verdict, visual structure/style, component/API,
  model/config/code, aggregate JSON, generated site data, generator, segdata, evidence artifact,
  README, other chapter, and dependency/lock.
- Raw/private corpus reads, runtime split reconstruction, fit/predict, external service, push, deploy,
  publication, or modification of historical/withdrawn evidence.
- The separate MFW topic-validity decision and v3.2 control-plane tasks.

### Stop / reclassification

- Any need to change an artifact, number, result direction, generated byte, generator, additional
  public source, component structure, raw/runtime evidence, or external state.
- Unknown overlapping WIP, new R3 factor, budget crossing, or unresolved review blocker after one
  correction pass.

## 5. Frozen acceptance and complexity tripwires

- Production/publication subsystems: one (`site`); production files: exactly one existing JSX source.
- Source LOC added/deleted ceiling: +30/-30; net source delta ceiling: +8.
- Test files: one existing file; test LOC added ceiling: 40.
- Task/handoff LOC added ceiling: 300.
- New file outside task, public entry point, dependency, framework/layer, state, abstraction, selector,
  component, data field, number or concept: 0.
- Aggregate/generated/evidence/config/model bytes and external writes: exactly 0.
- Review: one independent claim review and at most one bounded correction pass.

## 6. Domain contract

- Producer: active aggregate LBO artifact and rigor limitation; consumer: Sholokhov article wording.
- Grain: one held-out work/result versus one reference-label assumption; they are not the same claim.
- Key/cardinality: tested TD works are 1:1 held-out identities; reference anchor set is exactly three
  early solo works. No join to raw rows occurs in this task.
- Time/status: current public wording only; aggregate result bytes remain immutable here.
- NULL/delete/late arrival: none; absence of independent label provenance remains unknown, never pass.
- Ownership: evaluation owns split/result; reporting owns faithful wording; site renders it.
- Verification: exact anchor/held-out fields, retained caveat, banned absolute phrases, required narrow
  wording, and unchanged data expressions/artifacts/generated bytes.

## 7. Invariants

- INV-01: “not trained on tested works” is never rendered as “independent proof of reference labels”.
- INV-02: no early Don reference is called undisputed; mature controls such as `Поднятая целина` keep
  their narrower status where supported.
- INV-03: `3/4`, FPR, gradients, p-values, candidate list, direction and overall “leans Sholokhov but
  not proved” verdict do not change.
- INV-04: the accurate residual-circle caveat at the boundary remains explicit and adjacent to the
  key verdict, not hidden only in details.
- INV-05: no build/deploy/publication or evidence-byte mutation occurs.

## 8. Frozen wording changes

1. Test №1 anchors: “ранние рассказы и поздняя проза с корпусной меткой «Шолохов»”.
2. Four “бесспорные Донские рассказы/Шолохов” loci: replace with “размеченные/с меткой Шолохов”.
3. Test №3 note: verdict test excludes tested works but retains dependence on remaining labels.
4. Book test: “с исключением проверяемых работ из обучения”.
5. Key verdict heading: “без утечки проверяемых произведений”; remaining anchors are corpus-labelled.
6. Add adjacent sentence: this excludes tested-work leakage, not reference-label circularity.
7. Multi-test summary and predecessor comparison: use the same target-leakage/residual-label split.

## 9. Acceptance criteria

- [ ] AC-01 All ten inventoried overclaims are removed; no public `без замкнутого круга`, `без этого
  круга`, early-Don `бесспорн*`, or “претензии закрыты” remains.
- [ ] AC-02 The key verdict explicitly states both no tested-work leakage and residual dependence on
  reference labels; the detailed 1924–26 caveat remains.
- [ ] AC-03 Static gate checks exact active anchors/held-out TD set, forbidden/required phrases, and
  presence of the residual-circle caveat.
- [ ] AC-04 All JSX expressions/numbers and active `3/4` source binding remain unchanged.
- [ ] AC-05 `docs/*.json`, generated data/manifest, generator and segdata are byte-identical to baseline.
- [ ] AC-06 Targeted test, generator self-test, provenance, site build/render, relevant/full Python
  regression and release hygiene pass; skips/warnings/cannot-run are separate.
- [ ] AC-07 No deploy/push/publication/external write or raw/private data access occurs.

## 10. Verification and review

- Targeted: `tests/test_site_generator_contract.py` plus the exact new claim node.
- Static negative: banned absolute phrases absent; required target-exclusion/label-dependence phrases
  present; exact anchor and held-out sets read only from aggregate JSON.
- Site: generator `--self-test`; provenance; `npm ci`, `npm run gen`, `npm run build`; generated diff
  must remain empty. Dependency installation is verification-only and never committed.
- Regression: full Python suite; inventory/release hygiene; baseline diff over protected artifacts.
- Review: one clean-context review against AC/INV and exact baseline diff. Blocking only for remaining
  overclaim, changed meaning/number/artifact, unsupported wording, regression, safety or tripwire.

## 11. R3/publication boundary

- Local reversible source correction is classified R2. No external contract changes until a later
  push/deploy; that action is R3a and is not authorized here.
- Rollback: revert only the local task commits; no external state exists.

## 12. Documentation impact

- ADR/domain/runbook: none; this reconciles wording to existing evidence and creates no new semantics.
- Governance/roadmap: none; current limitation is already recorded.
- Handoff: record corrected claim and retain topic-study/control-plane gates.

## 13. Result

- Pending.

## 14. DoD references

- [ ] DOD-01 through DOD-05
- [ ] DOD-07 through DOD-12
- DOD-06/13/14/15: N/A unless reclassified.
