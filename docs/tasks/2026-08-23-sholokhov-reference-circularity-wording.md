# Sholokhov reference-circularity wording

## Metadata

- Task ID: `2026-08-23-sholokhov-reference-circularity-wording`
- Status: complete
- Owner: Dmitry Purtov (repository owner)
- Created: 2026-08-23
- Baseline commit: `67afbb65678698d9ab4aa4168d58748bb6983d4d`
- Target: attached `main`; local commits only
- Type / Size / Risk: implementation / M / R2
- Risk flags: mutation, scientific-claim
- Primary domain: reporting/publication
- Allowed cross-domain: site (one claim source); evaluation evidence (read-only aggregate contract)

## 1. Goal and decision

- Observable result: the public Sholokhov chapter distinguishes exclusion of tested works from
  training from the residual reference-label circularity of early Don anchors.
- Consumer: readers of the static article and future publication review.
- Decision unlocked: retain the directional `3/4 → Шолохову` result with its actual scope, without
  representing early reference labels as independently settled evidence.
- Success is not a new attribution result, weaker/stronger numerical verdict, artifact correction,
  model rerun, generated-data change, deployment, push, or publication.

The operator explicitly prioritized factual/model accuracy and autonomous bounded fixes. This task
changes only local publication source; no external write/deploy occurs. Any actual publication action
would be a separate R3a gate with immediate human approval.

## 4. Scope

## 10. Verification and review

- Targeted: `tests/test_site_generator_contract.py` plus the exact new claim node.
- Static negative: banned absolute phrases absent; required target-exclusion/label-dependence phrases
  present; exact anchor and held-out sets read only from aggregate JSON.
- Site: generator `--self-test`; provenance; `npm ci`, `npm run gen`, `npm run build`; generated diff
  must remain empty. Dependency installation is verification-only and never committed.
- Regression: full Python suite; inventory/release hygiene; baseline diff over protected artifacts.
- Review: one clean-context review against AC/INV and exact baseline diff. Blocking only for remaining
  overclaim, changed meaning/number/artifact, unsupported wording, regression, safety or tripwire.

## 13. Result

- Commits: wording/review candidate `61e20436`; the single bounded correction `ff7b5290`.
- Public correction: ten loci now say exactly what the aggregate evidence supports—tested TD/control
  works are excluded from training, while early reference works retain the corpus label “Sholokhov”.
  The key verdict states the residual reference-label circularity adjacently; the detailed 1924–26
  caveat remains. Early Don references are no longer called undisputed.
- Frozen meaning: all JSX data expressions, numbers, active `3/4` direction, FPR/gradient/p-values,
  candidate list and overall “leans Sholokhov, not proved” verdict are unchanged. `docs/*.json`, site
  generated data/manifest, generator and segdata are byte-identical to baseline.
- Independent review: **FAIL** on one AC-03 gate defect—the first test required the four expected TD
  IDs only as a subset and would accept a fifth. The one allowed correction filters every
  `tihiy_don_*` held-out identity and requires exact equality to `{1,2,3,4}`. Frozen verification
  passed afterward; no second-review PASS is claimed.
- Verification: claim/site contract `13 passed`; relevant publication/governance/release set `105
  passed` with two pre-existing invalid-escape warnings; full regression exited zero with 1,652
  collected tests, three executed environment/data skips plus the separately collection-skipped
  live-golden module, and the same two warnings. Generator self-test, provenance `93+1`, release
  hygiene, 300-path executable inventory and corrected protected-byte diff passed.
- Site runtime: locked `npm ci` installed 67 packages for verification; `npm run gen` reproduced
  generated bytes; `npm run build` transformed 55 modules and render-smoked 5 chapters. The only
  warning was the pre-existing >500 kB bundle-size advisory. No dependency/lock/source output was
  committed and no deploy/publication occurred.
- Tripwires: one site source +16/-14 (net +2); one existing test +31; task/handoff +208/-23;
  no number, data field, component, API, selector, dependency, framework, state, artifact, generated
  byte, additional public source or external write. Review/correction used `1/1`, `1/1`.
- Residual: actual runtime split/content-duplicate absence was not re-audited; the narrowed statement
  remains an aggregate-artifact claim. A future publish/deploy still requires a separate R3a approval.
