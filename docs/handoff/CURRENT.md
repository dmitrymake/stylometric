# Current Handoff

- State: **MFW topic mechanism confirmed; corrected-corpus effect unmeasured**
- Updated: 2026-08-23
- Verified baseline commit: `60c61101639bc954c489dca2d54f2efd5b4b0142`
- Branch/worktree at capture: attached `main`; candidate commit is forty-one commits ahead of
  `origin/main`; task/handoff closeout edits follow that baseline and do not predict their own commit.
- Active task: none
- Last completed task: `docs/tasks/2026-08-23-stylo-topic-validity-gate.md`
- Active standard: `docs/agentic/STANDARD.md` v1.3

This file only routes the next session. Scientific status and authorization remain owned by the
governance ledger, protocol, executable gates, and completed task evidence.

## 1. Verified current state

| Item | Verified status | Evidence owner |
|---|---|---|
| v3.2 preparation/frozen inputs | Owner-accepted evaluator input; exact candidate/context identities unchanged | preparation/reconciliation tasks |
| Evaluator receipt | Mandatory independent expectations and semantic recomputation implemented at `5eeae554`; exact JSON scalar aliases corrected | receipt-closure task |
| Receipt review | One review FAIL on AC-04 exact types; one allowed correction; frozen verification passed; no second-review PASS claimed | receipt-closure task |
| Topic validity | Synthetic MFW content-token mechanism confirmed; topic-strict control invariant; real LOBO effect unknown | topic-validity task |
| Production evaluator | Registry empty; candidate unregistered | `CONFIRMATORY_EVALUATOR_REGISTRY` |
| Freeze/preflight/authorization | `None` / absent / absent | runner + governance ledger |
| Confirmatory execution | Hard-disabled; no real v3.2 fit/predict occurred in the task | runner + receipt-closure task |
| Headline/publication | Not authorized; no site/public bytes changed | governance ledger |

## 2. Material change after the prior handoff

- A portable oracle proves that unrestricted MFW admits label-correlated content nouns for two noun
  pairs at two strengths, while fixed-list vectors are noun-swap invariant and remain sensitive to
  genuine function-word changes.
- Every supported `stylo` A0-A4 route reproduced clean/swapped accuracy `1.0/0.0` and flip rate `1.0`.
  An otherwise matching test-only `topic_strict` vectorizer had exact matrix/prediction invariance,
  flip rate `0.0`, and `topic_stress_delta=1.0`; repeat aggregate digests matched.
- One independent scientific review passed with zero blockers and no correction. Governance/roadmap
  now distinguish confirmed synthetic mechanism from unknown corrected-corpus magnitude and retain
  the unregistered/not-factology-ready boundary.
- Production Python, config/resources, protocol, model/evaluator/receipt, corpus/folds/results and site
  bytes are unchanged. Full 1,651-test collection exited zero with three executed environment/data
  skips, one separately collection-skipped live-golden module, and two pre-existing invalid-escape warnings. Py-compile,
  diff, release hygiene, and the 300-path executable inventory passed.

## 3. Next accuracy loop gate

Before production evaluator registration, explicitly authorize or R3b-reclassify a separately
versioned aggregate-only topic-validity study. The minimum decision-relevant run is corrected
LOBO-248, `stylo` A0 and A4 only, current versus `topic_strict` on identical ordered folds. Persist
only aggregate accuracy/delta, per-author transition counts, prediction-vector digests and bound
identities—never text, vocabulary, work IDs, probabilities, row predictions, checkpoints or official
v3.2 receipts/results. Accuracy alone does not select model semantics; an owner decision follows.

Separately, reporting/publication has a bounded wording finding: the public Sholokhov section says
“без замкнутого круга”/“лишь бесспорные работы”, while its active rigor artifact and later caveat
retain reference-label circularity. Correct it under a separate claim task; do not deploy or publish
without explicit publication authorization.

The earlier RunPlan/evaluator-registration/freeze/preflight boundary remains necessary after the
topic-validity decision. No current task grants that registration, freeze, execution, headline, or
publication authority.

## 4. Revalidation conditions

Revalidate before mutation if history does not contain `60c61101`, the topic/receipt tasks are not
complete, bound governance/source hashes differ, registry/freeze/preflight/authorization status
changes, the worktree contains unknown WIP, or a new R3/sensitive-data factor appears. Re-run the
topic oracle after any feature/factory change and the receipt oracle after any evaluator change.

## 5. Available and unavailable evidence

- Available: tracked Git/code/config/tests/governance/protocol, executable topic/receipt synthetic
  counterexamples, aggregate research artifacts, local Python 3.11.14 with spaCy 3.8.11, and release gates.
- Unavailable/not inspected for the completed task: raw/private corpus text as reportable evidence,
  a real v3.2 fit/predict/result, external services, GitHub Pages runtime, secrets, and production
  publication state.

## 6. References

- Topic task: `docs/tasks/2026-08-23-stylo-topic-validity-gate.md`
- Receipt task: `docs/tasks/2026-08-23-v3-2-receipt-semantic-closure.md`
- Evaluator task: `docs/tasks/2026-08-10-paired-audit-v3-2-evaluator.md`
- Frozen inputs: `docs/tasks/2026-08-11-v3-2-frozen-input-reconciliation.md`
- Scientific status: `research/governance/status_ledger.json`, `research/ROADMAP.md`
- Protocol: `research/work_balanced/paired_audit_protocol.md`
- Process: `docs/agentic/STANDARD.md`, `docs/handoff/README.md`
